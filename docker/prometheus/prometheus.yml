# Prometheus scrape config for IAD (Phase 12).
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: iad-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: iad-api
          environment: docker
