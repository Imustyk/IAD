"""Application service layer (Phase 5+).

Use-case orchestration that the frontend (Streamlit) and the backend
(FastAPI) both consume. Keeps UI thin and reuses identical business logic
across delivery channels.
"""
